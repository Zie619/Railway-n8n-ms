{
  "name": "ads-inspector-server",
  "version": "1.0.0",
  "description": "Tiny API to explore ad/trackers from a crawl JSON",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js --watch",
    "check": "node server.js --check"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "compression": "*",
    "cors": "*",
    "dotenv": "*",
    "express": "*",
    "express-rate-limit": "*",
    "helmet": "*",
    "morgan": "*",
    "swagger-ui-express": "*"
  }
}
